// ── Assistant identity ──────────────────────────────────────
// Rename the assistant here; everything else follows.
export const ASSISTANT_NAME = 'SCRAP'
export const ASSISTANT_EXPANSION = 'Salvaged Conversational Retro-Apocalyptic Processor'
export const ASSISTANT_TAGLINE = 'rebuilt · offline · unimpressed'

// Laugh clip text — tune here; changing this auto-invalidates the TTS cache.
export const LAUGH_TEXT = 'Ha. Ha.'

// Set to true to re-enable TTS audio for the laugh sequence once a TTS engine
// pronounces laughs convincingly.  When false, visuals play but no clip is
// fetched or cached.
export const LAUGH_AUDIO_ENABLED = false
